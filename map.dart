main(List<String> args) {
  // it's similar like python Dictionary
  var gift = {"1": "pen", "2": "paper", "3": "marker"};
  print(gift["2"]);

  // empty map
  var emp=Map();
  emp["first"]='Mango';
  print(emp["first"]);
}